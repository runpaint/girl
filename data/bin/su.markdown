Become a different user.

* `su` - Prompts for the root password which, if entered correctly, logs in
the user as 'root'.
* `su user` - Prompts for the password for the `user` username which, if
entered correctly, logs in the user as `user`.

Note: to execute a specific command as another user, see the `sudo` command.
